
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
new = { path = "../game", optional = true }

[features]
default = ["new"]
dylib = ["fyrox/dylib"]
